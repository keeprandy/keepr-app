# Copy to .local-env/staging.env and fill locally. Do not commit .local-env/.
EXPO_PUBLIC_SUPABASE_URL=https://nvtotcdsvijssokijnbn.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Only needed for migration commands. Use a direct or pooled staging database URL.
# Migration scripts must verify this URL resolves to nvtotcdsvijssokijnbn before running SQL.
SUPABASE_DB_URL=
